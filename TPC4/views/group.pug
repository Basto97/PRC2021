extends Layout
block content
    table.table
        thead
            tr
                th Elements
        tbody
            each e in group
                tr
                    td
                        a(href='/elems/' + e.Symbol)=e.Symbol